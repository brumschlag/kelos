{{- if .Values.telemetry.enabled }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: kelos-telemetry
  namespace: kelos-system
  labels:
    app.kubernetes.io/name: kelos
    app.kubernetes.io/component: telemetry
spec:
  schedule: "0 6 * * *"
  concurrencyPolicy: Replace
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      backoffLimit: 3
      activeDeadlineSeconds: 300
      template:
        spec:
          serviceAccountName: kelos-controller
          securityContext:
            runAsNonRoot: true
          {{- with .Values.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.affinity }}
          affinity:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          restartPolicy: OnFailure
          containers:
            - name: telemetry
              image: {{ .Values.controllerImage }}{{- if or .Values.controllerImageTag .Values.image.tag }}:{{ .Values.controllerImageTag | default .Values.image.tag }}{{- end }}
              {{- if .Values.image.pullPolicy }}
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              {{- end }}
              args:
                - --telemetry-report
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop:
                    - ALL
              resources:
                limits:
                  cpu: 100m
                  memory: 64Mi
                requests:
                  cpu: 10m
                  memory: 32Mi
{{- end }}
