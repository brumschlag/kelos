apiVersion: v1
kind: Secret
metadata:
  name: beads-dolt-creds
type: Opaque
stringData:
  # TODO: Replace with the password for the beads hub's Dolt remote.
  BEADS_DOLT_PASSWORD: "REPLACE-ME"
  # Optional — the beads CLI supplies its own default when this is omitted.
  BEADS_DOLT_USER: "sync"
